export type FilterUser = {
  label: string;
  value: string;
  count: number;
  email?: string | null;
  photoUrl?: string | null;
};
export type FilterTag = {
  label: string;
  value: string;
  count: number;
};
